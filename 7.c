// 7.Program to find the sum of the 2 numbers.
#include<stdio.h>
int main()
{
    int a,b, sum;
    printf("enter two values\n");
    scanf("%d %d", &a, &b);
    sum = a+b;
    printf("sum of the 2 numbers = %d\n", sum);
    return 0;
}
